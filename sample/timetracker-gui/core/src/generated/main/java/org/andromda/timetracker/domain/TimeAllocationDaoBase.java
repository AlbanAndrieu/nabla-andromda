// license-header java merge-point
//
// Attention: Generated code! Do not modify by hand! hibernate4=false hibernateVersion=3
// Generated by: hibernate/SpringHibernateDaoBase.vsl overrided in andromda-spring-cartridge.
//
package org.andromda.timetracker.domain;

import java.security.Principal;
import java.util.ArrayList;
import java.util.Collection;
import java.util.Iterator;
import java.util.LinkedHashSet;
import java.util.List;
import java.util.Set;
import org.andromda.spring.PaginationResult;
import org.andromda.timetracker.PrincipalStore;
import org.andromda.timetracker.PropertySearch;
import org.andromda.timetracker.Search;
import org.andromda.timetracker.SearchParameter;
import org.andromda.timetracker.vo.TimeAllocationVO;
import org.apache.commons.collections.CollectionUtils;
import org.apache.commons.collections.Transformer;
import org.hibernate.Criteria;
import org.hibernate.HibernateException;
import org.hibernate.NonUniqueResultException;
import org.hibernate.Query;
import org.hibernate.ScrollableResults;
import org.hibernate.Session;
import org.springframework.orm.hibernate3.HibernateCallback;
import org.springframework.orm.hibernate3.support.HibernateDaoSupport;

/**
 * <p>
 * Base Spring DAO Class: is able to create, update, remove, load, and find
 * objects of type <code>TimeAllocation</code>.
 * </p>
 *
 * @see TimeAllocation
 */
public abstract class TimeAllocationDaoBase
    extends HibernateDaoSupport
    implements TimeAllocationDao
{
    /**
     * {@inheritDoc}
     */
    @Override
    public Object get(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.get - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(TimeAllocationImpl.class, id);
        return transformEntity(transform, (TimeAllocation)entity);
    }
    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation get(Long id)
    {
        return (TimeAllocation)this.get(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object load(final int transform, final Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.load - 'id' can not be null");
        }
        final Object entity = this.getHibernateTemplate().get(TimeAllocationImpl.class, id);
        return transformEntity(transform, (TimeAllocation)entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation load(Long id)
    {
        return (TimeAllocation)this.load(TRANSFORM_NONE, id);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<TimeAllocation> loadAll()
    {
        return (Collection<TimeAllocation>) this.loadAll(TimeAllocationDao.TRANSFORM_NONE);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform)
    {
        return this.loadAll(transform, -1, -1);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int pageNumber, final int pageSize)
    {
        return this.loadAll(TimeAllocationDao.TRANSFORM_NONE, pageNumber, pageSize);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> loadAll(final int transform, final int pageNumber, final int pageSize)
    {
        try
        {
            final Criteria criteria = this.getSession(false).createCriteria(TimeAllocationImpl.class);
            if (pageNumber > 0 && pageSize > 0)
            {
                criteria.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                criteria.setMaxResults(pageSize);
            }
            final Collection<?> results = criteria.list();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * firstResult = (pageNumber - 1) * pageSize
     * @param pageNumber
     * @param pageSize
     * @return firstResult
     */
    protected int calculateFirstResult(int pageNumber, int pageSize)
    {
        int firstResult = 0;
        if (pageNumber > 0)
        {
            firstResult = (pageNumber - 1) * pageSize;
        }
        return firstResult;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation create(TimeAllocation timeAllocation)
    {
        return (TimeAllocation)this.create(TimeAllocationDao.TRANSFORM_NONE, timeAllocation);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(final int transform, final TimeAllocation timeAllocation)
    {
        if (timeAllocation == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.create - 'timeAllocation' can not be null");
        }
        this.getHibernateTemplate().save(timeAllocation);
        return this.transformEntity(transform, timeAllocation);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public Collection<TimeAllocation> create(final Collection<TimeAllocation> entities)
    {
        return (Collection<TimeAllocation>) create(TimeAllocationDao.TRANSFORM_NONE, entities);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Collection<?> create(final int transform, final Collection<TimeAllocation> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.create - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (TimeAllocation entity : entities)
                    {
                        create(transform, entity);
                    }
                    return null;
                }
            });
        return entities;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation create(
        TimePeriod timePeriod)
    {
        return (TimeAllocation)this.create(TimeAllocationDao.TRANSFORM_NONE, timePeriod);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        TimePeriod timePeriod)
    {
        TimeAllocation entity = new TimeAllocationImpl();
        entity.setTimePeriod(timePeriod);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation create(
        Task task,
        Timecard timecard,
        TimePeriod timePeriod)
    {
        return (TimeAllocation)this.create(TimeAllocationDao.TRANSFORM_NONE, task, timecard, timePeriod);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object create(
        final int transform,
        Task task,
        Timecard timecard,
        TimePeriod timePeriod)
    {
        TimeAllocation entity = new TimeAllocationImpl();
        entity.setTask(task);
        entity.setTimecard(timecard);
        entity.setTimePeriod(timePeriod);
        return this.create(transform, entity);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(TimeAllocation timeAllocation)
    {
        if (timeAllocation == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.update - 'timeAllocation' can not be null");
        }
        this.getHibernateTemplate().update(timeAllocation);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void update(final Collection<TimeAllocation> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.update - 'entities' can not be null");
        }
        this.getHibernateTemplate().executeWithNativeSession(
            new HibernateCallback()
            {
                public Object doInHibernate(Session session)
                    throws HibernateException
                {
                    for (TimeAllocation entity : entities)
                    {
                        update(entity);
                    }
                    return null;
                }
            });
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(TimeAllocation timeAllocation)
    {
        if (timeAllocation == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.remove - 'timeAllocation' can not be null");
        }
        this.getHibernateTemplate().delete(timeAllocation);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Long id)
    {
        if (id == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.remove - 'id' can not be null");
        }
        TimeAllocation entity = this.get(id);
        if (entity != null)
        {
            this.remove(entity);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void remove(Collection<TimeAllocation> entities)
    {
        if (entities == null)
        {
            throw new IllegalArgumentException(
                "TimeAllocation.remove - 'entities' can not be null");
        }
        for (TimeAllocation entity : entities)
        {
            this.getHibernateTemplate().delete(entity);
        }
    }
    /**
     * Allows transformation of entities into value objects
     * (or something else for that matter), when the <code>transform</code>
     * flag is set to one of the constants defined in <code>TimeAllocationDao</code>, please note
     * that the {@link #TRANSFORM_NONE} constant denotes no transformation, so the entity itself
     * will be returned.
     * <p>
     * This method will return instances of these types:
     * <ul>
     *   <li>{@link TimeAllocation} - {@link #TRANSFORM_NONE}</li>
     *   <li>{@link TimeAllocationVO} - {@link #TRANSFORM_TIMEALLOCATIONVO}</li>
     * </ul>
     *
     * If the integer argument value is unknown {@link #TRANSFORM_NONE} is assumed.
     *
     * @param transform one of the constants declared in {@link TimeAllocationDao}
     * @param entity an entity that was found
     * @return the transformed entity (i.e. new value object, etc)
     * @see TimeAllocationDao#transformEntity(int, TimeAllocation)
     */
    public Object transformEntity(final int transform, final TimeAllocation entity)
    {
        Object target = null;
        if (entity != null)
        {
            switch (transform)
            {
                case TRANSFORM_TIMEALLOCATIONVO :
                    target = toTimeAllocationVO(entity);
                    break;
                case TimeAllocationDao.TRANSFORM_NONE : // fall-through
                default:
                    target = entity;
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void transformEntities(final int transform, final Collection<?> entities)
    {
        switch (transform)
        {
            case TRANSFORM_TIMEALLOCATIONVO :
                toTimeAllocationVOCollection(entities);
                break;
            case TimeAllocationDao.TRANSFORM_NONE : // fall-through
                default:
                // do nothing;
        }
    }

    /**
     * @see TimeAllocationDao#toEntities(Collection)
     */
    public void toEntities(final Collection<?> results)
    {
        if (results != null)
        {
            CollectionUtils.transform(results, this.ENTITYTRANSFORMER);
        }
    }

    /**
     * This anonymous transformer is designed to transform report query results
     * (which result in an array of entities) to {@link TimeAllocation}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer ENTITYTRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof Object[])
                {
                    result = toEntity((Object[])input);
                }
                else if (input instanceof TimeAllocation)
                {
                    result = input;
                }
                return result;
            }
        };

    /**
     * @param row
     * @return TimeAllocation
     */
    protected TimeAllocation toEntity(Object[] row)
    {
        TimeAllocation target = null;
        if (row != null)
        {
            final int numberOfObjects = row.length;
            for (int ctr = 0; ctr < numberOfObjects; ctr++)
            {
                final Object object = row[ctr];
                if (object instanceof TimeAllocation)
                {
                    target = (TimeAllocation)object;
                    break;
                }
            }
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({"unchecked"})
    public final Collection<TimeAllocationVO> toTimeAllocationVOCollection(Collection<?> entities)
    {
        Collection<TimeAllocationVO> result = new ArrayList<TimeAllocationVO>();
        if (entities != null)
        {
            CollectionUtils.transform(entities, this.TIMEALLOCATIONVO_TRANSFORMER);
            result.addAll((Collection<? extends TimeAllocationVO>) entities);
        }
        return result;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public final TimeAllocationVO[] toTimeAllocationVOArray(Collection<?> entities)
    {
        TimeAllocationVO[] result = null;
        if (entities != null)
        {
            // Unchecked transformation: reuses entities Collection
            final Collection collection = new ArrayList(entities);
            this.toTimeAllocationVOCollection(collection);
            result = (TimeAllocationVO[]) collection.toArray(new TimeAllocationVO[collection.size()]);
        }
        return result;
    }

    /**
     * Default implementation for transforming the results of a report query into a value object. This
     * implementation exists for convenience reasons only. It needs only be overridden in the
     * {@link TimeAllocationDaoImpl} class if you intend to use reporting queries.
     * @param row
     * @return toTimeAllocationVO(this.toEntity(row))
     * @see TimeAllocationDao#toTimeAllocationVO(TimeAllocation)
     */
    protected TimeAllocationVO toTimeAllocationVO(Object[] row)
    {
        return this.toTimeAllocationVO(this.toEntity(row));
    }

    /**
     * This anonymous transformer is designed to transform entities or report query results
     * (which result in an array of objects) to {@link TimeAllocationVO}
     * using the Jakarta Commons-Collections Transformation API.
     */
    private Transformer TIMEALLOCATIONVO_TRANSFORMER =
        new Transformer()
        {
            public Object transform(Object input)
            {
                Object result = null;
                if (input instanceof TimeAllocation)
                {
                    result = toTimeAllocationVO((TimeAllocation)input);
                }
                else if (input instanceof Object[])
                {
                    result = toTimeAllocationVO((Object[])input);
                }
                return result;
            }
        };

    /**
     * {@inheritDoc}
     */
    @Override
    public final void timeAllocationVOToEntityCollection(Collection<?> instances)
    {
        if (instances != null)
        {
            for (final Iterator<?> iterator = instances.iterator(); iterator.hasNext();)
            {
                // - remove an objects that are null or not of the correct instance
                if (!(iterator.next() instanceof TimeAllocationVO))
                {
                    iterator.remove();
                }
            }
            CollectionUtils.transform(instances, this.TimeAllocationVOToEntityTransformer);
        }
    }

    private final Transformer TimeAllocationVOToEntityTransformer =
        new Transformer()
        {
            public Object transform(Object input)
            {
                return timeAllocationVOToEntity((TimeAllocationVO)input);
            }
        };


    /**
     * {@inheritDoc}
     */
    @Override
    public void toTimeAllocationVO(
        TimeAllocation source,
        TimeAllocationVO target)
    {
        target.setId(source.getId());
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocationVO toTimeAllocationVO(final TimeAllocation entity)
    {
        TimeAllocationVO target = null;
        if (entity != null)
        {
            target =  new TimeAllocationVO();
            this.toTimeAllocationVO(entity, target);
        }
        return target;
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public void timeAllocationVOToEntity(
        TimeAllocationVO source,
        TimeAllocation target,
        boolean copyIfNull)
    {
    }

    /**
     * Gets the current <code>principal</code> if one has been set,
     * otherwise returns <code>null</code>.
     *
     * @return the current principal
     */
    protected Principal getPrincipal()
    {
        return PrincipalStore.get();
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings({ "unchecked" })
    public PaginationResult search(final int transform, final int pageNumber, final int pageSize, final Search search)
    {
        try
        {
            search.setPageNumber(pageNumber);
            search.setPageSize(pageSize);
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), TimeAllocationImpl.class, search);
            final List results = propertySearch.executeAsList();
            this.transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), propertySearch.getTotalCount());
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public PaginationResult search(final int pageNumber, final int pageSize, final Search search)
    {
        return this.search(TimeAllocationDao.TRANSFORM_NONE, pageNumber, pageSize, search);
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Set<?> search(final int transform, final Search search)
    {
        try
        {
            final PropertySearch propertySearch = new PropertySearch(
                this.getSession(false), TimeAllocationImpl.class, search);
            final Set<?> results = propertySearch.executeAsSet();
            this.transformEntities(transform, results);
            return results;
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    @SuppressWarnings("unchecked")
    public Set<TimeAllocation> search(final Search search)
    {
        return (Set<TimeAllocation>) this.search(TimeAllocationDao.TRANSFORM_NONE, search);
    }

    /**
     * Executes and returns the given Hibernate queryObject as a {@link PaginationResult} instance.
     * @param queryObject
     * @param transform
     * @param pageNumber
     * @param pageSize
     * @return PaginationResult
     */
    @SuppressWarnings({ "unchecked" })
    protected PaginationResult getPaginationResult(
        final Query queryObject,
        final int transform, int pageNumber, int pageSize)
    {
        try
        {
            final ScrollableResults scrollableResults = queryObject.scroll();
            scrollableResults.last();
            int totalCount = scrollableResults.getRowNumber();
            totalCount = totalCount >= 0 ? totalCount + 1 : 0;
            if (pageNumber > 0 && pageSize > 0)
            {
                queryObject.setFirstResult(this.calculateFirstResult(pageNumber, pageSize));
                queryObject.setMaxResults(pageSize);
            }
            // Unchecked transformation because Set object is reused, cannot be strongly typed.
            Set results = new LinkedHashSet(queryObject.list());
            transformEntities(transform, results);
            return new PaginationResult(results.toArray(new Object[results.size()]), totalCount);
        }
        catch (HibernateException ex)
        {
            throw super.convertHibernateAccessException(ex);
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public TimeAllocation searchUniqueTimePeriod(final TimePeriod timePeriod)
    {
        final Search search=new Search(
            new SearchParameter[]{
                new SearchParameter("timePeriod",timePeriod,SearchParameter.EQUAL_COMPARATOR)
            }
        );

        final Set<TimeAllocation> searchResult=this.search(search);
        switch(searchResult.size())
        {
            case 0: return null;
            case 1: return searchResult.iterator().next();
            default: throw new NonUniqueResultException(searchResult.size());
        }
    }

    /**
     * {@inheritDoc}
     */
    @Override
    public Object searchUniqueTimePeriod(final int transform, final TimePeriod timePeriod)
    {
        final TimeAllocation uniqueEntity=this.searchUniqueTimePeriod(timePeriod);
        if(uniqueEntity == null)
        {
            return null;
        }
        return transformEntity(transform, uniqueEntity);
    }

    // spring-hibernate-dao-base merge-point
}